<div class="vh-80 d-flex justify-content-center align-items-center">
  <div class="col-md-4 p-5 shadow-sm border rounded-5 border-primary" style="margin-top: 60px;">
    <h2 class="text-center mb-4 text-primary">Create a new Chatroom</h2>
    <%= simple_form_for @chatroom do |f| %>
      <div class="mb-3">
        <%= f.input :name, label_html: { class: "form-label" } %>
        <%= f.input :photo, as: :file %>
      </div>
      <div class="chatroom-button">
        <%= f.button :submit, 'Create Chatroom', class: 'create_btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
